<Window x:Class="APR_TEST.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:APR_TEST.Views"
        xmlns:vm="clr-namespace:APR_TEST.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <Image Source="{Binding CurrentFile.DisplayImage}" Stretch="Uniform" Width="600" Height="350"/>
        <Button Content="커맨드" Command="{Binding LoadImageCommand}" Width="200" Height="40"/>
    </StackPanel>
</Window>
